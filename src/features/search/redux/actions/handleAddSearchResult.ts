import SearchResult from "../../types/SearchResult"
import State from "../state"


export default function handleAddSearch(state: State, action: { payload: { query: string, result: SearchResult } }) {
  
}
